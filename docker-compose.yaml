version: '3.8'

services:
  cloud-customer-app:
    build:
      context: .
      dockerfile: Dockerfile.dockerfile # Verweise auf das benannte Dockerfile
    ports:
      - "8080:8080"
    networks:
      - cloud-net

networks:
  cloud-net:
    driver: bridge

